# URL da API NestJS (server-side e client-side apontam para o mesmo backend local)
NEXT_PUBLIC_API_URL="http://localhost:3001"

# Auth.js
AUTH_SECRET="CHANGE_ME_dev_only_secret"
AUTH_URL="http://localhost:3000"
